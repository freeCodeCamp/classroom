import Head from 'next/head';
import Navbar from '../../components/navbar';
import Link from 'next/link';
import OrgOnboardingModal from '../../components/orgOnboardingModal';
import { useState } from 'react';
import { createPortal } from 'react-dom';

export default function Home() {
  let [isOpen, setIsOpen] = useState(false);
  const handleModal = () => {
    setIsOpen(!isOpen);
  };
  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name='description' content='Generated by create next app' />
        <link rel='icon' href='/favicon.ico' />
      </Head>
      <Navbar>
        <div className='border-solid border-2 pl-4 pr-4'>
          <Link href={'/classes'}>Classes</Link>
        </div>
        <div className='border-solid border-2 pl-4 pr-4'>
          <Link href={'/'}> Menu</Link>
        </div>
      </Navbar>

      <div className={'p-6 ml-20 mr-20'}>
        <h1 className='box-content bg-fcc-gray-15 h-100 w-100 p-4 border-4 border-fcc-gray-90 text-3xl'>
          We are so happy to see you here, please click this button below to
          start your onboarding process!
        </h1>
      </div>
      <div className='flex justify-center'>
        <button
          onClick={() => setIsOpen(true)}
          id='openOnboardingModal'
          className='hover:bg-[#ffbf00] shadow-lg border-solid border-color: black; border-[1px] pl-4 pr-4 bg-fcc-primary-yellow text-black'
        >
          Get started with Classroom
        </button>
      </div>
      {isOpen &&
        createPortal(
          <OrgOnboardingModal toggleModalFromChild={handleModal} />,
          document.body
        )}
    </>
  );
}
